{
  "entities": {
    "Config": {
      "title": "Application Configuration",
      "description": "Global settings for themes, music, and videos",
      "type": "object",
      "properties": {
        "themeBgs": { "type": "object", "description": "Mapping of themes to background image URLs" },
        "themeMusics": { "type": "object", "description": "Mapping of themes to playlists of audio URLs" },
        "themeVideos": { "type": "object", "description": "Mapping of themes to playlists of video URLs" },
        "masterVolume": { "type": "number", "description": "Global audio volume (0-1)" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["themeBgs", "themeMusics", "themeVideos", "masterVolume"]
    }
  },
  "firestore": {
    "app/settings": {
      "schema": "Config",
      "description": "Primary application configuration document"
    }
  }
}
